# Описание проекта TranscriptorMiniApp

## Краткое описание проекта

**TranscriptorMiniApp** — это веб-приложение для записи и транскрибации аудио, разработанное как Telegram Mini App. Приложение позволяет пользователям записывать голосовые сообщения, загружать их на сервер для обработки и получать текстовую транскрипцию записи.

### Основной функционал:
- 🎤 Запись аудио через микрофон браузера
- ⏸️ Пауза и возобновление записи
- ⏹️ Остановка записи
- 🔊 Визуализация аудио в реальном времени (эквалайзер с 20 полосами)
- ▶️ Прослушивание записанного аудио с возможностью перемотки
- 📤 Загрузка аудио на сервер для транскрибации
- 📝 Отображение и редактирование текста транскрипции
- 💾 Сохранение транскрипции в файл и localStorage
- 🎨 Адаптивный дизайн с поддержкой темной темы Telegram
- 📱 Оптимизация для мобильных устройств

---

## Архитектура проекта

### Технологический стек:
- **React 19.2.0** — UI библиотека
- **Vite 7.2.2** — сборщик и dev-сервер
- **Telegram Web App SDK** — интеграция с Telegram
- **Web Audio API** — работа с аудио и визуализация
- **MediaRecorder API** — запись аудио
- **Fetch API** — HTTP запросы к бэкенду

### Архитектурные принципы:
1. **Компонентный подход** — приложение разбито на переиспользуемые React-компоненты
2. **Сервисный слой** — бизнес-логика вынесена в отдельные сервисы
3. **Разделение ответственности** — UI, логика и API разделены
4. **Конфигурация через переменные окружения** — настройки API вынесены в конфиг

### Структура архитектуры:

```
┌─────────────────────────────────────────┐
│         Presentation Layer              │
│  (React Components + CSS)               │
├─────────────────────────────────────────┤
│         Business Logic Layer            │
│  (Component State Management)           │
├─────────────────────────────────────────┤
│         Service Layer                   │
│  (API Services)                          │
├─────────────────────────────────────────┤
│         Configuration Layer             │
│  (API Config, Test Data)                │
└─────────────────────────────────────────┘
```

---

## Структура файлов проекта

### Корневая директория:
```
TranscriptorMiniApp/
├── public/              # Статические файлы
├── src/                 # Исходный код приложения
├── package.json         # Зависимости и скрипты
├── vite.config.js       # Конфигурация Vite
├── index.html           # HTML шаблон
└── README.md            # Документация
```

### Директория `src/`:

#### **Основные файлы:**
- `main.jsx` — точка входа приложения, инициализация React и Telegram Web App
- `App.jsx` — главный компонент приложения, управление состоянием и роутинг
- `App.css` — стили главного компонента
- `index.css` — глобальные стили и CSS переменные

#### **Директория `components/`** — UI компоненты:

**AudioRecorder.jsx / AudioRecorder.css** (773 строки / 557 строк)
- Главный компонент для записи и обработки аудио
- Управляет всем жизненным циклом записи: старт, пауза, остановка
- Интегрирует визуализацию аудио, плеер и транскрипцию
- Обрабатывает загрузку на сервер и получение результатов

**AudioWaves.jsx / AudioWaves.css** (54 строки / 131 строка)
- Компонент визуализации аудио (эквалайзер)
- Отображает 20 полос частот в реальном времени
- Анимированная визуализация в состоянии покоя

**Button.jsx / Button.css** (43 строки / 422 строки)
- Универсальный компонент кнопки
- Поддерживает варианты: record, pause, resume, stop, upload, cancel
- Круглые и прямоугольные варианты
- Анимации и эффекты hover

**LoadingScreen.jsx / LoadingScreen.css** (30 строк / 91 строка)
- Экран загрузки при старте приложения
- Отображает аватар и имя пользователя из Telegram
- Анимированный спиннер

**Иконки:**
- `MicrophoneIcon.jsx` — SVG иконка микрофона (150x150)
- `PauseIcon.jsx` — SVG иконка паузы (50x50)
- `StopIcon.jsx` — SVG иконка остановки (50x50)

#### **Директория `services/`** — бизнес-логика:

**api.js** (248 строк)
- Базовый сервис для HTTP запросов
- Методы: GET, POST, PUT, DELETE, POST FormData
- Обработка ошибок и авторизации
- Поддержка токенов из localStorage или Telegram

**audioApi.js** (66 строк)
- Специализированный сервис для работы с аудио API
- Методы:
  - `uploadAudio(file, options)` — загрузка аудио файла
  - `getTranscription(id)` — получение статуса и результата транскрипции

#### **Директория `config/`** — конфигурация:

**apiConfig.js** (19 строк)
- Конфигурация API endpoints и базового URL
- Настройки таймаутов
- Использует переменные окружения (VITE_API_BASE_URL)

**testData.js** (22 строки)
- Тестовые данные пользователя для локальной разработки
- Функция `isLocalDevelopment()` — определение режима разработки

### Директория `public/`:
- `microfon.svg` — SVG иконка микрофона
- `testavatar.jpeg` — тестовый аватар для разработки
- `vite.svg` — иконка Vite

---

## Детальное описание компонентов

### 1. App.jsx
**Роль:** Главный компонент приложения

**Функциональность:**
- Инициализация Telegram Web App SDK
- Настройка темы (светлая/темная) в зависимости от Telegram
- Получение данных пользователя (имя, фото) из Telegram или тестовых данных
- Управление экраном загрузки (2 секунды)
- Передача состояния в дочерние компоненты

**Состояние:**
- `userName`, `userPhoto` — данные пользователя
- `audioData` — данные для визуализации
- `isRecording` — флаг записи
- `hasAudioBlob` — наличие записанного аудио
- `isLoading` — состояние загрузки

### 2. AudioRecorder.jsx
**Роль:** Центральный компонент для записи и обработки аудио

**Основные функции:**

**Запись аудио:**
- `startRecording()` — запрос доступа к микрофону, создание MediaRecorder и AudioContext
- `pauseRecording()` — пауза записи
- `resumeRecording()` — возобновление записи
- `stopRecording()` — остановка записи и создание Blob
- `cancelRecording()` — отмена записи и очистка состояния

**Анализ аудио:**
- `analyzeAudio()` — анализ частот через Web Audio API
- Создание данных для 20-полосного эквалайзера
- Использование `requestAnimationFrame` для плавной анимации

**Работа с сервером:**
- `uploadAudio()` — загрузка аудио файла на сервер
- `checkTranscriptionStatus(id)` — периодическая проверка статуса транскрипции (каждые 3 секунды)
- Обработка различных форматов ответа сервера

**Плеер:**
- Управление воспроизведением записанного аудио
- Прогресс-бар с возможностью перемотки (поддержка touch и mouse событий)
- Отображение текущего времени и длительности

**Транскрипция:**
- Отображение текста транскрипции в редактируемом textarea
- Сохранение транскрипции в файл и localStorage
- Уведомление о готовности транскрипции

**Состояние компонента:**
- 28 различных состояний для управления UI и логикой
- Использование refs для избежания проблем с замыканиями

### 3. AudioWaves.jsx
**Роль:** Визуализация аудио данных

**Функциональность:**
- Отображение 20 полос эквалайзера
- Режимы:
  - **Реальные данные** — во время записи показывает реальные частоты
  - **Анимация** — в покое показывает анимированные волны
  - **Статичное состояние** — минимальная высота при отсутствии данных

### 4. Button.jsx
**Роль:** Переиспользуемый компонент кнопки

**Варианты:**
- `record` — красная кнопка записи (круглая, 160px)
- `pause` — оранжевая кнопка паузы
- `resume` — зеленая кнопка продолжения
- `stop` — серая кнопка остановки
- `upload` — светло-синяя кнопка загрузки
- `cancel` — светло-красная кнопка отмены

**Особенности:**
- Поддержка иконок (SVG, эмодзи, React компоненты)
- Круглые и прямоугольные варианты
- Анимации и эффекты hover
- Адаптивность для мобильных устройств

### 5. LoadingScreen.jsx
**Роль:** Экран загрузки при старте

**Функциональность:**
- Отображение аватара пользователя (если доступен)
- Приветствие с именем пользователя
- Анимированный спиннер загрузки
- Плавные анимации появления элементов

---

## Стили и дизайн

### Цветовая схема:
- **Основной:** Белый фон (#ffffff)
- **Текст:** Темно-серый (#213547)
- **Запись:** Красный (rgba(255, 68, 68, 0.85))
- **Пауза:** Оранжевый (rgba(255, 170, 0, 0.85))
- **Продолжить:** Зеленый (rgba(0, 170, 0, 0.85))
- **Остановить:** Серый (rgba(102, 102, 102, 0.85))
- **Эквалайзер:** Голубой (rgba(135, 206, 250, ...))

### Особенности стилей:
- **Прозрачность и blur эффекты** — использование `backdrop-filter` для стеклянного эффекта
- **Адаптивность** — медиа-запросы для экранов 320px, 375px, 768px
- **Safe Area** — поддержка безопасных зон iOS (notch, home indicator)
- **Анимации** — плавные переходы и keyframe анимации
- **Touch-friendly** — минимальные размеры кнопок 44px/52px для удобного нажатия

### Шрифты:
- **Основной:** Nunito (Google Fonts)
- **Fallback:** системные шрифты (-apple-system, BlinkMacSystemFont, и т.д.)

---

## API интеграция

### Endpoints:
- **POST** `/audio/upload` — загрузка аудио файла
  - Формат: FormData с полем `file`
  - Ответ: `{ id: number, status: string, ... }`

- **GET** `/audio/transcription/{id}` — получение статуса и результата транскрипции
  - Ответ: `{ transcriptionResult: string, progressPercent: number, transcriptionStatus: number, ... }`

### Обработка ответов:
- Поддержка различных форматов ответа сервера
- Обработка ошибок 404, 400 (транскрипция еще обрабатывается)
- Периодическая проверка статуса каждые 3 секунды до получения результата

### Конфигурация:
- Базовый URL: `https://transcribate-audio-api.odonta.burtimaxbot.ru`
- Настраивается через переменную окружения `VITE_API_BASE_URL`

---

## Особенности реализации

### 1. Управление состоянием записи
- Использование refs (`isRecordingRef`, `isPausedRef`) для избежания проблем с замыканиями в асинхронных операциях
- Синхронизация состояния между компонентами через props и callbacks

### 2. Анализ аудио
- Использование Web Audio API (`AudioContext`, `AnalyserNode`)
- Настройки для чувствительности: `fftSize: 512`, `smoothingTimeConstant: 0.2`
- Нормализация данных для яркой визуализации (множитель 3.5, минимум 15%)

### 3. Обработка ошибок
- Graceful degradation при отсутствии доступа к микрофону
- Повторные попытки при ошибках сети
- Информативные сообщения об ошибках для пользователя

### 4. Оптимизация производительности
- Очистка таймеров и анимаций при размонтировании компонентов
- Освобождение ресурсов AudioContext и MediaStream
- Использование `requestAnimationFrame` для плавной анимации

### 5. Telegram интеграция
- Автоматическое определение режима (локальная разработка / продакшн)
- Использование Telegram Web App SDK для получения данных пользователя
- Поддержка темной темы Telegram
- Отключение вертикальных свайпов для закрытия приложения

---

## Зависимости проекта

### Production:
- `react` (^19.2.0) — UI библиотека
- `react-dom` (^19.2.0) — React для DOM
- `@twa-dev/sdk` (^8.0.2) — Telegram Web App SDK

### Development:
- `vite` (^7.2.2) — сборщик
- `@vitejs/plugin-react` (^5.1.0) — плагин React для Vite
- `eslint` (^9.39.1) — линтер
- TypeScript типы для React

---

## Скрипты

- `npm run dev` — запуск dev-сервера
- `npm run build` — сборка для продакшна
- `npm run preview` — предпросмотр production сборки
- `npm run lint` — проверка кода линтером

---

## Браузерные API

Проект использует следующие браузерные API:
- **MediaRecorder API** — запись аудио
- **Web Audio API** — анализ частот
- **MediaDevices.getUserMedia()** — доступ к микрофону
- **Blob API** — работа с бинарными данными
- **URL.createObjectURL()** — создание URL для аудио
- **localStorage** — сохранение данных локально
- **Fetch API** — HTTP запросы

---

## Поддержка устройств

- ✅ Мобильные устройства (iOS, Android)
- ✅ Планшеты
- ✅ Десктопные браузеры
- ✅ Telegram Web App (встроенный браузер)
- ✅ Адаптивный дизайн для экранов от 320px

---

## Безопасность

- Проверка разрешений на доступ к микрофону
- Обработка ошибок доступа
- Валидация данных перед отправкой на сервер
- Очистка URL объектов для предотвращения утечек памяти

---

## Будущие улучшения

Возможные направления развития:
- Поддержка нескольких языков транскрибации
- История записей и транскрипций
- Экспорт в различные форматы (PDF, DOCX)
- Улучшенная обработка ошибок сети
- Offline режим с кэшированием
- Поддержка видео транскрибации

